# Auto-generated / volatile context — kept on disk across sessions, but not version-controlled
# (regenerated by hooks; tracking them would churn git history). Remove these lines if you want
# the accumulated tool-failure lessons committed to the repo.
README.md
tool_lessons/*.md
