# Copy to secret.env (gitignored) and fill in. Consumed by the overlay's secretGenerator.
# Keys must match the Deployment's secretKeyRefs. Provide at least one LLM key.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
HF_TOKEN=
